import { Size } from "ngx-spinner";

const tamanho: Size = "medium";
const bdColor = "rgba(0, 0, 0, 0.8)";
const color = "#fff";
const fullScren = true;

export const ConfiguracaoSpinner = Object.freeze({
  tamanho,
  bdColor,
  color,
  fullScren
});
